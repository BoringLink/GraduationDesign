# AGENT.md (Legacy Compatibility)

## Status

本文件为兼容入口；项目主规则已迁移至根目录 `AGENTS.md` 及其分层子目录规则文件。

## Mandatory Rule (Preserved)

1. 编译论文时，必须使用 `@latex/thesis/build.sh` 执行构建，不得直接调用其他编译命令。

## Canonical Rule Source

- Primary: `./AGENTS.md`
- Child overrides: nearest path-local `**/AGENTS.md`
